---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: quicksync
  name: ersatztv-pvc
  labels:
    app: ersatztv
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
